# berlingreen-app-config

This repository is used by the BerlinGreen mobile app to define firmware version offerings.
The app fetches `app-config.json` from it.

Served at https://config.berlingreen.com/app-config.json.

To change which firmware updates the app offers, edit `app-config.json` and push to `main`.
